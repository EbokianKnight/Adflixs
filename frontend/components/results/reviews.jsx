var React = require('react');
var PropTypes = React.PropTypes;

var ReviewResults = React.createClass({

  render: function() {
    return (
      <div>
        This will Render Search Components
      </div>
    );
  }

});

module.exports = ReviewResults;
